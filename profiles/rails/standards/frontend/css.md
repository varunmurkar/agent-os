# Frontend CSS (Project Overlay)

Canonical reusable guidance lives in `skills/frontend/references/css-core.md`.

## Project-Specific Overlay

- Project token naming and CSS file layout are defined in project docs when present.
