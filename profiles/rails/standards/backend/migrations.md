# Backend Migration Standards (Project Overlay)

Canonical reusable guidance lives in `skills/backend/references/migrations-core.md`.

## Project-Specific Overlay

- Use Rails migration tooling and naming generated by project conventions.
- Keep migration execution compatible with this repository's CI/deployment flow.
- For Postgres projects, load `skills/supabase-postgres-best-practices/SKILL.md` and relevant `schema-*`/`query-*`/`security-*` references before migration design is finalized.
