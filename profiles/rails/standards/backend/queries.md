# Backend Query Standards (Project Overlay)

Canonical reusable guidance lives in `skills/backend/references/querying-core.md`.

## Project-Specific Overlay

- No additional project-specific query rules are defined here.
- For Postgres query/index work, load `skills/supabase-postgres-best-practices/SKILL.md` and relevant `query-*`/`data-*`/`monitor-*` references.
